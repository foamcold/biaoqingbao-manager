# Flask App Configuration (Copy to .env and fill in)
# Flask 应用配置（复制到 .env 文件并填写）

# WARNING: DO NOT COMMIT YOUR ACTUAL .env FILE TO VERSION CONTROL!
# Add .env to your .gitignore file.
# 警告：不要将包含真实密钥的 .env 文件提交到版本控制！
# 将 .env 添加到你的 .gitignore 文件中。

# --- Security 安全 ---
# Generate a strong, random secret key.
# Use: python -c 'import secrets; print(secrets.token_hex(16))'
# 生成一个强随机的密钥。
# 使用命令: python -c 'import secrets; print(secrets.token_hex(16))'
SECRET_KEY=

# Set a strong admin password
# 设置一个强的管理员密码
ADMIN_PASSWORD=

# --- Flask Environment Flask 环境 ---
# Set to 'production' for deployment, 'development' for local testing
# 部署时设置为 'production'，本地测试时设置为 'development'
FLASK_ENV=development
# Set to '0' for production, '1' for local testing (enables debug mode)
# 生产环境设置为 '0'，本地测试设置为 '1' (启用调试模式)
FLASK_DEBUG=1

# --- Application Specific 应用特定 ---
# Optional: Override the default emoticons folder path if needed
# 可选：如果需要，覆盖默认的表情包文件夹路径
# EMOTICONS_FOLDER=emoticons